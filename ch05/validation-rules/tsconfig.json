{
  "compilerOptions": {
    // Including "DOM" gives access to console methods.
    "lib": ["ES2022", "DOM"],
    "outDir": "./dist",
    "rootDir": "./src",
    "skipLibCheck": true, // avoids "Cannot find namespace 'JSX'"
    "strict": true,
    "target": "ES2022" // "ESNext" does not work with decorators!
  }
}
