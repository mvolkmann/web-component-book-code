# ch04/validation-rules

The steps to build and run this demo differ from some others in the ch04 directory
because vite-node doesn't yet support the `accessor` keyword.

To run this:

1. Enter `npm install`.
2. Enter `npm run build`.
3. Enter `npm start`.
