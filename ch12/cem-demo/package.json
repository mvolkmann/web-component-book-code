{
  "name": "cem-demo",
  "scripts": {
    "cem": "cem analyze --litelement",
    "cem:watch": "cem analyze --litelement --watch",
    "dev": "vite",
    "cem:wca": "wca analyze *.{js,ts} --format json --outFile custom-elements2.json",
    "readme": "wca analyze *.{js,ts} --outFile README.md",
    "reinstall": "rm -rf node_modules package-lock.json && npm install",
    "view": "wca view --format vscode *.{js,ts}"
  },
  "devDependencies": {
    "@custom-elements-manifest/analyzer": "^0.11.0",
    "vite": "^7.3.1",
    "web-component-analyzer": "^2.0.0"
  },
  "customElements": "custom-elements.json",
  "dependencies": {
    "api-viewer-element": "^0.6.4",
    "lit": "^3.3.2"
  }
}
