{
  "name": "wrec-ssr-demo",
  "scripts": {
    "build:client": "vite build",
    "build:server": "vite build --ssr",
    "build": "npm run build:client && npm run build:server",
    "clean": "rm -rf dist",
    "dev": "node --import ./dom-setup.js dist/server.js",
    "dev-save": "node dist/server.js"
  },
  "type": "module",
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.11",
    "hono": "^4.12.5",
    "linkedom": "^0.18.12",
    "wrec": "^0.20.16"
  }
}
