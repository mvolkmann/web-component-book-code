# fast-ssr-demo

This project demonstrates using server-side rendering
with FAST web components. To run this:

- Enter `npm install`.
- Enter `npm run build`.
- Browse localhost:3000.
- Click the "Greet" button to see SSR components.
